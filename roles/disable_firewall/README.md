# disable_firewall

Disables Windows Firewall on all profiles (Domain, Private, Public) for lab environments.

## Requirements

- Windows target host

## Role Variables

None required.

## Example

```yaml
roles:
  - professor_moody.ludus_scorch.disable_firewall
```
